// function to create a front and back of a basic card.
function BasicCard (front, back) {
    this.front = front;
    this.back = back;
}

console.log("Basic Card loaded.")

// export the module for the basic card
module.exports = BasicCard;